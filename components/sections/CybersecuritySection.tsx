'use client';
import { motion, useReducedMotion } from 'framer-motion';
import { maskReveal, staggerIn } from '@/lib/motion';

export default function CybersecuritySection(){const reduce=useReducedMotion(); const items=['Ransomware Trends','Cloud Security','Data Breach Prevention','Compliance'];return <section className='bg-[#0b1019] px-6 py-20'><div className='mx-auto max-w-7xl'><h2 className='font-[family-name:var(--font-space)] text-4xl font-bold text-transparent bg-gradient-to-r from-orange-400 to-cyan-400 bg-clip-text'>Cybersecurity Spotlight</h2><motion.div className='mt-8 grid gap-4 md:grid-cols-2' variants={staggerIn(0.1)} initial={reduce?undefined:'hidden'} whileInView={reduce?undefined:'visible'} viewport={{once:true,amount:0.2}}>{items.map((i)=><motion.article key={i} variants={maskReveal} whileHover={reduce?undefined:{y:-5}} className='rounded-2xl border border-slate-700 bg-[#0f1420] p-6 hover:border-cyan-400/70 hover:shadow-[0_0_24px_rgba(0,180,240,.25)]'><h3 className='text-xl text-white'>{i}</h3><p className='mt-2 text-slate-400'>Practical frameworks and managed controls to keep your attack surface continuously reduced.</p></motion.article>)}</motion.div></div></section>}
